import path from 'path';

export const config = {
    adaProjectFile: path.resolve(__dirname, '../spark_ada_project/main.gpr'),
    adaSourceDir: path.resolve(__dirname, '../spark_ada_project/src')
};

